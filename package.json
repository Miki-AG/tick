{
  "name": "tick",
  "version": "0.1.0",
  "private": true,
  "description": "Local file-based issue tracker.",
  "scripts": {
    "test": "node test/tick/run.js"
  },
  "engines": {
    "node": ">=18"
  }
}
